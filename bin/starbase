#!/usr/bin/env node

const { createStarbaseCli } = require('../dist/src/cli');

createStarbaseCli()
  .parseAsync(process.argv)
  .catch(err => {
    console.error(err);
    process.exitCode = 1;
  })
